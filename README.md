This repository has two files "series_visualizatoin.py" and "draw.py"

The former helps visualize the fourier series

The latter will draw an image using epicycles and the fourier transform algorithm 
